# 2026-04-23 Daily Log

## setting.vue - 自定义按钮去掉，slider 常住
- 去掉了 list_swap 里 define 按钮的按钮形态
- 改为在 4 个尺寸按钮下方常住一个 `.define-slider-row` 横排 div（label + slider）
- 点击该 div 触发 `switchSwap({ size: 'define' })`，激活时边框变为 up-color
- slider 本身用 @click.stop 阻止冒泡，拖动不重复触发

## setting.vue - 自定义按钮与 slider 合并（旧方案，已废弃）
- 文件：`/src/pages/pump/components/setting.vue`
- 将 `list_swap` 中的"自定义"按钮（`size==='define'`）和底部独立的 slider 行合并为一个元素
- 实现：按钮未选中时显示正常按钮样式（swap icon + 文字），选中时切换为横排 flex 布局（label + slider）
- 新增 `.define-btn` CSS 类：`flex-direction: column`（未选中）/ `flex-direction: row`（选中时 .active）
- `.slider-wrapper` width 从固定 60% 改为 100%（由父容器 flex-1 控制宽度）
- `switchSwap` 参数类型 `value` 改为 optional 修复 TS 报错
